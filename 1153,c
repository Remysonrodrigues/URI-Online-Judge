#include <stdio.h>
#include <stdlib.h>

int fat(int n){
    if(n == 1) return 1;
    else return n*fat(n-1);
}

int main()
{
    int n;

    scanf("%i", &n);
    printf("%i\n", fat(n));
    return 0;
}
